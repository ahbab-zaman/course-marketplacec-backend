// User Role Enum
enum Role {
  ADMIN
  INSTRUCTOR
  STUDENT
}

// Auth Provider Enum
enum AuthProvider {
  LOCAL
  GOOGLE
}

// OTP Type Enum
enum OtpType {
  VERIFY_EMAIL
  LOGIN
  RESET_PASSWORD
}

// Store Status Enum
enum CourseStatus {
  DRAFT
  PENDING_REVIEW
  PUBLISHED
  REJECTED
  ARCHIVED
}

enum EnrollmentStatus {
  ACTIVE
  REFUNDED
  REVOKED
}

enum PaymentStatus {
  PENDING
  SUCCEEDED
  FAILED
  REFUNDED
}

enum VideoProvider {
  CLOUDFLARE_STREAM
  MUX
  BUNNY_STREAM
}
