// OTP Model
model OTP {
  id        String   @id @default(uuid())
  email     String
  code      String // Hashed
  type      OtpType
  expiresAt DateTime
  isUsed    Boolean  @default(false)
  createdAt DateTime @default(now())

  @@map("otp")
}
